# 🤖 AI-powered FAQ Support Bot (Telegram)

---

## 📌 Описание проекта

Демонстрационный Telegram-бот технической поддержки, использующий **семантическое сопоставление FAQ** для автоматических ответов на вопросы пользователей.

Бот отвечает **только при достаточной уверенности**, а при сомнении корректно переводит диалог на оператора.  
Проект демонстрирует практичный и безопасный подход к автоматизации первой линии поддержки **без генерации “ответов из головы”**.

---

## 🎯 Цели проекта

- автоматизировать ответы на типовые вопросы поддержки;
- снизить нагрузку на операторов;
- обеспечить контроль качества ответов;
- показать воспроизводимый ML/NLP-пайплайн;
- предоставить готовый кейс для портфолио.

---

## 🧠 Ключевая идея

- **FAQ** — это знания (ответ хранится один раз);
- **вариации вопросов** — это язык пользователя;
- сопоставление выполняется через **семантические эмбеддинги + cosine similarity**.

Если бот не уверен в корректности ответа, он **не отвечает автоматически**, а передаёт запрос оператору.

---

## 🏗 Архитектура решения

variants.csv
↓
Sentence Embeddings
↓
Cosine Similarity
↓
Threshold Logic
│
├─→ confident → FAQ answer
└─→ not confident → operator


Дополнительно применяется фильтрация общих / панических запросов до этапа NLP.

---

## 📂 Структура проекта

demo/
├── .env.example
├── .env
├── requirements.txt
├── README.md
│
├── data/
│ ├── faq.csv
│ ├── variants.csv
│ └── splits.json
│
├── artifacts/
│ ├── embeddings.npy
│ ├── items.csv
│ └── meta.json
│
├── src/
│ ├── init.py
│ ├── config.py
│ ├── dataio.py
│ ├── embedder.py
│ ├── index.py
│ ├── retriever.py
│ ├── metrics.py
│ └── bot.py
│
└── scripts/
├── build_index.py
└── eval.py


---

## 🛠 Используемые технологии

- **Python 3.10+**
- **aiogram** (Telegram Bot API)
- **sentence-transformers**
- NumPy, pandas
- scikit-learn
- python-dotenv

---

## 📊 Данные

- `faq.csv` — 50 канонических вопросов и ответов (SaaS-тематика)
- `variants.csv` — 6 вариаций на каждый вопрос (≈300 записей)
- `splits.json` — параметры train/test разбиения

Все данные синтетические и используются исключительно для демонстрационных целей.

---

## 📈 Качество модели

Результаты на test-split:

- **accuracy@1 ≈ 0.94**
- **precision ≈ 0.94**
- **recall ≈ 0.94**
- **coverage** управляется параметром `THRESHOLD`

Качество системы в первую очередь определяется **качеством формулировок данных**, а не сложностью модели.

---

## ⚙️ Конфигурация

Файл `.env`:

```env
MODEL_NAME=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
THRESHOLD=0.75
TOP_K=5
TELEGRAM_BOT_TOKEN=your_token_here

---

## ▶️ Запуск проекта

1. Установка зависимостей
```
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```
2. Построение индекса
```
python -m scripts.build_index
```
3. Оценка качества
```
python -m scripts.eval
```
4. Запуск Telegram-бота
```
python -m src.bot
```
---

## 🤖 Поведение бота

Уверенные запросы
Бот возвращает ответ из FAQ, если:
similarity ≥ threshold;
запрос содержит конкретный объект или действие.
Неуверенные / общие запросы

Бот переводит диалог на оператора, если:
уверенность ниже порога;
запрос слишком общий или панический;
отсутствует явная предметная конкретика.

---

## 🧪 Примеры запросов

Автоматический ответ:

«Как сменить пароль?»
«Не приходит письмо с подтверждением»
«Как удалить пользователя из команды?»

Перевод к оператору:

«У меня проблема с аккаунтом»
«Ничего не работает»
«Помогите срочно»

---

## 🧩 Масштабируемость

Проект легко расширяется за счёт:
добавления новых FAQ без изменения кода;
увеличения числа вариаций вопросов;
смены модели эмбеддингов;
изменения порога уверенности;
подключения логирования запросов;
интеграции с CRM или тикет-системами.

---

## 🚀 Потенциальные улучшения

логирование и анализ пользовательских запросов;
приоритизация и кластеризация неуверенных кейсов;
интерактивное уточнение вопросов;
inline-режим Telegram;
мультиязычная поддержка;
веб-интерфейс для управления FAQ;
A/B-тестирование threshold.